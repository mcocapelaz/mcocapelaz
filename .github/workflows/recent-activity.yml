name: Update Recent Activity

on:
  schedule:
    - cron: "0 */6 * * *"  # Cada 6 horas
  workflow_dispatch:       # Para ejecutar manualmente
  push:
    branches: [ main ]     # También cuando hagas push

jobs:
  update-activity:
    runs-on: ubuntu-latest
    name: Actualizar actividad reciente
    
    steps:
      - name: 📥 Checkout del repositorio
        uses: actions/checkout@v4
        
      - name: 🔄 Actualizar actividad reciente
        uses: mcocapelaz/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          COMMIT_MSG: '🤖 Actualizar actividad reciente'
          MAX_LINES: 8
          COMMIT_NAME: 'GitHub Activity Bot'
          COMMIT_EMAIL: 'activity-bot@github.com'

